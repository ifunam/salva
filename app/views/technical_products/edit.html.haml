%h3= t('.title')
= simple_form_for @technical_product, :as => :technical_product, :url => technical_product_path, :html => { :multipart => true, :id => 'record'} do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), technical_products_path
