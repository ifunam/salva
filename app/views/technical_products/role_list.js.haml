.role_list

  .span-6.last
    %b=t(:role_selection)
    = link_to_close_author_list

  .span-6.last
    %ul
      - @userroles.each do |record|
        %li=link_to record.name, '#', 'data-remote-resource' => add_user_technical_product_path(:id => @technical_product.id, :userrole_id => record.id, :format => :js)
