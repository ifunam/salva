%h3= t('.title')
= simple_form_for @tutorial_committee, :url => tutorial_committees_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:save)
    = link_to t(:cancel), tutorial_committees_path