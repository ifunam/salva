.span-17.last
  %h3= t('.title')

#submit_buttons
  = link_to t(:new), new_tutorial_committee_path

= render :partial => 'search_form'

#collection_header
  .span-1="&nbsp;".html_safe
  .span-11=t('.title')
  .action=t(:show)
  .action=t(:edit)
  .span1.last=t(:del)

#collection_records
  = render :partial => 'record', :collection => @tutorial_committees

= render :partial => 'collection_footer'

= paginate(@tutorial_committees)

#submit_buttons
  = link_to t(:new), new_tutorial_committee_path
