%h3= t('.title')
= simple_form_for @advice, :as => :advice, :url => institutional_advices_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:save)
    = link_to t(:cancel), institutional_advices_path
