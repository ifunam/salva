%h3=t('.title')
= simple_form_for(@city, :remote => true, :html => {:id => 'new_city', 'data-class-name' => 'city'}) do |f|
  = render :partial => 'form', :locals => { :f => f}
  #dialog_submit_buttons
    = f.submit t(:save), :data => { confirm: t(:new_record_confirmation) }
