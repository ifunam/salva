-  @user_genericworks = user_role(genericwork, :user_genericworks, current_user.id)
= f.simple_fields_for :user_genericworks, @user_genericworks do |uf|
  - if !f.object.new_record?
    = uf.hidden_field :id
    = uf.hidden_field :genericwork_id, :value => genericwork.id
  = uf.association :userrole, :using => :name, :as => :select, :input_html => { :class => 'chosen-select' }
