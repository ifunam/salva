%h3= t('.title')
= simple_form_for @thesis, :as => :thesis, :url => thesis_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), thesis_path
