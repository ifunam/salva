%tbody{:id =>'collection'}
  = render :partial => 'record', :collection => @theses
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @theses, :id => 'ajaxed_paginator'
