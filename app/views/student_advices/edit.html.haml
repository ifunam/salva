%h3= t('.title')
= simple_form_for @advice, :as => :advice, :url => student_advice_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), student_advices_path
