%div{ :class => 'span-14 section_left last' } 
  %h3= t('roleinseminaries.new.title')
  = simple_form_for(@roleinseminary, :url => admin_roleinseminaries_path) do |f|
    = render :partial => 'form', :locals => { :f => f}
    %div{ :class => 'span-14 buttons_left last' }
      = f.submit  t(:save), :class => "ui-state-default ui-corner-all button"
      = link_as_button t(:cancel), admin_roleinseminaries_path