%div{ :class => 'span-14 section_left last' }
  %h3= t('languagelevels.edit.title') 
  = simple_form_for(@languagelevel, :languagelevel, :url => admin_languagelevel_path) do |f|
    = render :partial => 'form', :locals => { :f => f}
    %div{ :class => 'span-14 buttons_left last' }
      = f.submit  t(:update), :class => "ui-state-default ui-corner-all button"
      = link_as_button t(:cancel), admin_languagelevels_path
