%tbody{:id =>'collection'}
  = render :partial => 'languagelevel', :collection => @languagelevels
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @languagelevels, :id => 'ajaxed_paginator'
