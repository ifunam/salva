%tbody{:id =>'collection'}
  = render :partial => 'stimuluslevel', :collection => @stimuluslevels
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @stimuluslevels, :id => 'ajaxed_paginator'
