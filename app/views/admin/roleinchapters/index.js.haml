%tbody{:id =>'collection'}
  = render :partial => 'roleinchapter', :collection => @roleinchapters
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @roleinchapters, :id => 'ajaxed_paginator'
