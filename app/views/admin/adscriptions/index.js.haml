%tbody{:id =>'collection'}
  = render :partial => 'adscription', :collection => @adscriptions
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @adscriptions, :id => 'ajaxed_paginator'
