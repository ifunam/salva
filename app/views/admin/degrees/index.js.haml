%tbody{:id =>'collection'}
  = render :partial => 'degree', :collection => @degrees
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @degrees, :id => 'ajaxed_paginator'
