%div{ :class => 'span-14 section_left last' }
  %h3= t('degrees.show.title')
  - show_for @degree do |u|

    = u.attribute :name, :wrapper_html => {:class => 'span-14 last'}


%div{ :class => 'span-14 buttons_left last' }
  = link_as_button t(:edit), edit_admin_degree_path(:id => @degree.id)
  = link_as_button t(:back), admin_degrees_path
