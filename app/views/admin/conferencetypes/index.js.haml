%tbody{:id =>'collection'}
  = render :partial => 'conferencetype', :collection => @conferencetypes
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @conferencetypes, :id => 'ajaxed_paginator'
