%div{ :class => 'span-14 section_left last' }
  %h3= t('activitygroups.show.title')
  - show_for @activitygroup do |u|

    = u.attribute :name, :wrapper_html => {:class => 'span-14 last'}


%div{ :class => 'span-14 buttons_left last' }
  = link_as_button t(:edit), edit_admin_activitygroup_path(:id => @activitygroup.id)
  = link_as_button t(:back), admin_activitygroups_path
