%tbody{:id =>'collection'}
  = render :partial => 'state', :collection => @states
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @states, :id => 'ajaxed_paginator'
