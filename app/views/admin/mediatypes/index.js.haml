%tbody{:id =>'collection'}
  = render :partial => 'mediatype', :collection => @mediatypes
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @mediatypes, :id => 'ajaxed_paginator'
