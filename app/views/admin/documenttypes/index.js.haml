%tbody{:id =>'collection'}
  = render :partial => 'documenttype', :collection => @documenttypes
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @documenttypes, :id => 'ajaxed_paginator'
