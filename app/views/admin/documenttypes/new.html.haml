%div{ :class => 'span-14 section_left last' } 
  %h3= t('documenttypes.new.title')
  = simple_form_for(@documenttype, :url => admin_documenttypes_path) do |f|
    = render :partial => 'form', :locals => { :f => f}
    %div{ :class => 'span-14 buttons_left last' }
      = f.submit  t(:save), :class => "ui-state-default ui-corner-all button"
      = link_as_button t(:cancel), admin_documenttypes_path