%tbody{:id =>'collection'}
  = render :partial => 'booktype', :collection => @booktypes
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @booktypes, :id => 'ajaxed_paginator'
