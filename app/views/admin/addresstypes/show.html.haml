%div{ :class => 'span-14 section_left last' }
  %h3= t('addresstypes.show.title')
  - show_for @addresstype do |u|

    = u.attribute :name, :wrapper_html => {:class => 'span-14 last'}


%div{ :class => 'span-14 buttons_left last' }
  = link_as_button t(:edit), edit_admin_addresstype_path(:id => @addresstype.id)
  = link_as_button t(:back), admin_addresstypes_path
