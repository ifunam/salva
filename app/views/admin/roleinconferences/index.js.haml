%tbody{:id =>'collection'}
  = render :partial => 'roleinconference', :collection => @roleinconferences
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @roleinconferences, :id => 'ajaxed_paginator'
