%div{ :class => 'span-14 section_left last' }
  %h3= t('idtypes.show.title')
  - show_for @idtype do |u|

    = u.attribute :name, :wrapper_html => {:class => 'span-14 last'}


%div{ :class => 'span-14 buttons_left last' }
  = link_as_button t(:edit), edit_admin_idtype_path(:id => @idtype.id)
  = link_as_button t(:back), admin_idtypes_path
