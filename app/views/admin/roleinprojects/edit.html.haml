%div{ :class => 'span-14 section_left last' }
  %h3= t('roleinprojects.edit.title') 
  = simple_form_for(@roleinproject, :roleinproject, :url => admin_roleinproject_path) do |f|
    = render :partial => 'form', :locals => { :f => f}
    %div{ :class => 'span-14 buttons_left last' }
      = f.submit  t(:update), :class => "ui-state-default ui-corner-all button"
      = link_as_button t(:cancel), admin_roleinprojects_path
