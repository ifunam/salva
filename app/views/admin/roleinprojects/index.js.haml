%tbody{:id =>'collection'}
  = render :partial => 'roleinproject', :collection => @roleinprojects
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @roleinprojects, :id => 'ajaxed_paginator'
