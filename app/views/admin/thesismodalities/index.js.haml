%tbody{:id =>'collection'}
  = render :partial => 'thesismodality', :collection => @thesismodalities
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @thesismodalities, :id => 'ajaxed_paginator'
