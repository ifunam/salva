%tbody{:id =>'collection'}
  = render :partial => 'coursegrouptype', :collection => @coursegrouptypes
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @coursegrouptypes, :id => 'ajaxed_paginator'
