%tbody{:id =>'collection'}
  = render :partial => 'roleinbook', :collection => @roleinbooks
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @roleinbooks, :id => 'ajaxed_paginator'
