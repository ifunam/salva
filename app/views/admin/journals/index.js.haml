%tbody{:id =>'collection'}
  = render :partial => 'journal', :collection => @journals
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @journals, :id => 'ajaxed_paginator'
