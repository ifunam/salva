%tbody{:id =>'collection'}
  = render :partial => 'talktype', :collection => @talktypes
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @talktypes, :id => 'ajaxed_paginator'
