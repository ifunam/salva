%div{ :class => 'span-14 section_left last' }
  %h3= t('talktypes.show.title')
  - show_for @talktype do |u|

    = u.attribute :name, :wrapper_html => {:class => 'span-14 last'}


%div{ :class => 'span-14 buttons_left last' }
  = link_as_button t(:edit), edit_admin_talktype_path(:id => @talktype.id)
  = link_as_button t(:back), admin_talktypes_path
