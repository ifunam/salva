%tbody{:id =>'collection'}
  = render :partial => 'bookchaptertype', :collection => @bookchaptertypes
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @bookchaptertypes, :id => 'ajaxed_paginator'
