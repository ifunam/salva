%tbody{:id =>'collection'}
  = render :partial => 'roleinconferencetalk', :collection => @roleinconferencetalks
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @roleinconferencetalks, :id => 'ajaxed_paginator'
