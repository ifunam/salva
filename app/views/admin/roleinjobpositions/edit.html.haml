%div{ :class => 'span-14 section_left last' }
  %h3= t('roleinjobpositions.edit.title') 
  = simple_form_for(@roleinjobposition, :roleinjobposition, :url => admin_roleinjobposition_path) do |f|
    = render :partial => 'form', :locals => { :f => f}
    %div{ :class => 'span-14 buttons_left last' }
      = f.submit  t(:update), :class => "ui-state-default ui-corner-all button"
      = link_as_button t(:cancel), admin_roleinjobpositions_path
