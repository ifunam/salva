%tbody{:id =>'collection'}
  = render :partial => 'period', :collection => @periods
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @periods, :id => 'ajaxed_paginator'
