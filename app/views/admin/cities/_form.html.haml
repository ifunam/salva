%div{ :class => 'span-8 last' } 
  = render :partial => "/shared/errors", :object => f.object.errors

= f.input :name, :as => :string, :input_html => { :size => 20 }, :autocomplete => "off", :wrapper_class => 'span-8 last'

%div{:class=> 'span-8 last'}
  = f.label :state_id, :required => true
  = f.collection_select :state_id, State.all, :id, :name, {:prompt => true, :selected => 2, :required => true}
