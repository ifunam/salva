%div{ :class => 'span-8 last' } 
  = render :partial => "/shared/errors", :object => f.object.errors


= f.input :name, :as => :string, :wrapper_class => 'span-8 last' 


%div{:class=> 'span-8'}
  = f.label :activitygroup_id, :required => true 
  = f.collection_select :activitygroup_id,  Activitygroup.all, :id, :name, {:prompt => true, :selected => 2, :required => true}

