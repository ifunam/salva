%tbody{:id =>'collection'}
  = render :partial => 'language', :collection => @languages
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @languages, :id => 'ajaxed_paginator'
