%tbody{:id =>'collection'}
  = render :partial => 'thesisstatus', :collection => @thesisstatuses
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @thesisstatuses, :id => 'ajaxed_paginator'
