-  @user_seminaries = user_role(seminary, :user_seminaries, current_user.id)
= f.simple_fields_for :user_seminaries, @user_seminaries do |uf|
  - if !f.object.new_record?
    = uf.hidden_field :id
    = uf.hidden_field :seminary_id, :value => seminary.id
  = uf.hidden_field :user_id, :value => current_user.id
  = uf.collection_select :roleinseminary_id, Roleinseminary.not_attendee, :id, :name, :prompt => true