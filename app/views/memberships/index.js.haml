%tbody{:id =>'collection'}
  = render :partial => 'record', :collection => @memberships
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @memberships, :id => 'ajaxed_paginator'
