-  @user_newspaperarticles = user_role(newspaperarticle, :user_newspaperarticles, current_user.id)
= f.simple_fields_for :user_newspaperarticles, @user_newspaperarticles do |uf|
  - if !f.object.new_record?
    = uf.hidden_field :id
    = uf.hidden_field :newspaperarticle_id, :value => newspaperarticle.id
  = uf.hidden_field :user_id, :value => current_user.id
  = uf.hidden_field :ismainauthor, :value => true
