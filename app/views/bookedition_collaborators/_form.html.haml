-  @bookedition_collaborators = user_role(book_collaboration, :bookedition_roleinbooks, current_user.id)
= f.simple_fields_for :bookedition_roleinbooks, @bookedition_collaborators do |uf|
  - if !f.object.new_record?
    = uf.hidden_field :id
    = uf.hidden_field :bookedition_id, :value => book_collaboration.id
  = uf.hidden_field :user_id, :value => current_user.id
  = uf.input :roleinbook_id, :as => :select, :collection => Roleinbook.collaborators, :as => :radio, :required => true,  :wrapper_class => 'radio-set'

