%h3= t('.title')
= simple_form_for @thesis_examination, :as => :thesis_examination, :url => thesis_examination_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), thesis_examination_path
