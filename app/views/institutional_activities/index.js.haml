#collection_records
  = render :partial => 'record', :collection => @institutional_activities

= render :partial => 'collection_footer'

= will_paginate @institutional_activities, :id => 'ajaxed_paginator'