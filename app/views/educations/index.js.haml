%tbody{:id =>'collection'}
  = render :partial => 'record', :collection => @educations
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @educations, :id => 'ajaxed_paginator'
