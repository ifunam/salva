.span-1= checkbox_to_delete(record)
.span-8
  = record.to_s
  = image_tag('associated_record.png', :class => 'associated_record_indicator', :title => t(:associated))
.associated_authors
  .authors_size= record.users.size
  = link_to_user_list user_list_seminary_path(:id => record.id, :format => 'js')
.author_action
  = link_to_del_author t(:del_role), del_user_seminary_path(:id => record.id, :format => :js)
.action
  = link_to_show seminary_path(:id => record.id)
.action
  =link_to_edit edit_seminary_path(:id => record.id)
.action
  = link_to_delete record, seminary_path(:id => record.id)
