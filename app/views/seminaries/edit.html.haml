%h3= t('.title')
= simple_form_for @seminary, :as => :seminary, :url => seminary_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), seminaries_path