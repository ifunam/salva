.span-17.last
  %h3= t('.title')

=# render :partial => 'search_form'

#collection_header
  .span-1="&nbsp;".html_safe
  .span-11="Áreas de investigación"
  .action=t(:show)
  .action=t(:edit)
  .span1.last=t(:del)

#collection_records
  = render :partial => 'record', :collection => @user_knowledge_areas

= render :partial => 'collection_footer'

= paginate(@user_knowledge_areas)

#submit_buttons
  = link_to t(:new), new_user_knowledge_area_path
