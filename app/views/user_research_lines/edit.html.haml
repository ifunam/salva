%h3= t('.title')
= simple_form_for @researchline, :as => :researchline, :url => user_research_line_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), user_research_line_path