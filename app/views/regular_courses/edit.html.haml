%h3= t('.title')
= simple_form_for @regular_course, :as => :regular_course, :url => regular_course_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), regular_course_path
