.dialog-section-with-border-and-bg
  .row
    = f.input :name, :as => :string, :input_html => { :size => 38 }, :label_html => { :class => 'span-8' }, :label => 'Nombre de la institución'
  .row
    = f.input :abbrev, :as => :string, :input_html => { :size => 10 }
  .row
    = f.hidden_field :institutiontitle_id, :value => 1
  .row
    = f.hidden_field :institutiontype_id, :value => 1
  .row
    = f.association :country, :using => :name, :input_html  => {:class => 'chosen-select' }, :selected => f.object.country_id || 484
