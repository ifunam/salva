%h3= t('.title')
= simple_form_for @course, :url => course_attendees_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:save)
    = link_to t(:cancel), course_attendees_path