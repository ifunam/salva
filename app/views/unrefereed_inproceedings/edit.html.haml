%h3= t('.title')
= simple_form_for @inproceeding, :as => :inproceeding, :url => unrefereed_inproceeding_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), unrefereed_inproceedings_path
