Su cuenta en el SALVA ha sido <<<activada>>:

  Login: <%= @user.login %>
  Contraseña: ***********

Para iniciar una sesión use el siguiente enlace en el navegador de 
su preferencia:

<%= url_for(:controller => :session, :only_path => false) %>
