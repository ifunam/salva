%tbody{:id =>'collection'}
  = render :partial => 'record', :collection => @jobpositions
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @jobpositions, :id => 'ajaxed_paginator'
