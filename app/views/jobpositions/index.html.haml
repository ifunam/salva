.span-17.last
  %h3= t('.title')

= render :partial => 'search_form'

#collection_header
  .span-1="Id"
  .span-11=t('.jobpositions')
  .action=t(:show)
  .action=t(:edit)
  .action.last=t(:del)

#collection_records
  = render :partial => 'record', :collection => @jobpositions

#collection_footer
  = link_as_button t(:delete_all_selected), '#', :class_button => 'dangerous_button', :id => 'destroy_selected_records', :confirm => t(:delete_all_confirm_question)

= will_paginate(@jobpositions)

#submit_buttons
  = link_to t(:add), new_jobposition_path
