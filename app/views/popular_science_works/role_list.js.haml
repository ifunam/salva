.role_list

  .span-6.last
    %b=t(:role_selection)
    = link_to_close_author_list

  .span-6.last
    %ul
      - @userroles.each do |record|
        %li=link_to record.name, '#', 'data-remote-resource' => add_user_popular_science_work_path(:id => @genericwork.id, :userrole_id => record.id, :format => :js)
