- if user.students.count > 0
  %h3 Students
  %table
    %tr
      %th Fotografía
      %th Nombre
      %th Correo electrónico
      %th Periodo
    %tbody
      = render :partial => 'student', :collection => user.students
