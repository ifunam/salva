%h3= t('.title')
= simple_form_for @activity, :url => other_activities_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:save)
    = link_to t(:cancel), other_activities_path