%tbody{:id =>'collection'}
  = render :partial => 'article', :collection => @articles
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @articles, :id => 'ajaxed_paginator'
