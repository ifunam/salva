.role_list

  .span-6.last
    %b=t(:role_selection)
    = link_to_close_author_list

  .span-6.last
    %ul
      - Roleinchapter.all.each do |record|
        %li= link_to record.name, '#', 'data-remote-resource' => add_user_book_chapter_path(:id => @book_chapter.id, :roleinchapter_id => record.id, :format => :js)
