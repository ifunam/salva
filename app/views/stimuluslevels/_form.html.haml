.dialog-section-with-border-and-bg
  .row
    = f.input :name, :as => :string, :input_html => { :size => 38, :required => true }
  .row
    = f.association :stimulustype, :using => :name, :input_html => {:prompt => true, :selected => 2, :required => true, :class => 'chosen-select'}
