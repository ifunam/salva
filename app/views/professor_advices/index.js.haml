%tbody{:id =>'collection'}
  = render :partial => 'record', :collection => @advices
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @advices, :id => 'ajaxed_paginator'
