%h3= t('.title')
= simple_form_for @conference, :as => :conference, :url => conference_organizer_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), conference_organizers_path
