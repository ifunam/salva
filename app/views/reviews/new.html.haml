%h3= t('.title')
= simple_form_for @review, :url => reviews_path, :html => { :id => 'record'} do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:save)
    = link_to t(:cancel), reviews_path