%tbody{:id =>'collection'}
  = render :partial => 'record', :collection => @reviews
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @reviews, :id => 'ajaxed_paginator'
