%h3= t('.title')
= simple_form_for @user_journal, :url => user_journals_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:save)
    = link_to t(:cancel), user_journals_path