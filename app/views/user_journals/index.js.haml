%tbody{:id =>'collection'}
  = render :partial => 'record', :collection => @user_journals
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @user_journals, :id => 'ajaxed_paginator'
