-  @chapterinbook_collaborators = user_role(book_chapter, :chapterinbook_roleinchapters, current_user.id)
= f.simple_fields_for :chapterinbook_roleinchapters, @chapterinbook_collaborators do |uf|
  - if !f.object.new_record?
    = uf.hidden_field :id
    = uf.hidden_field :chapterinbook_id, :value => book_chapter.id
  = uf.hidden_field :user_id, :value => current_user.id
  = uf.input :roleinchapter_id, :as => :radio, :collection => Roleinchapter.all, :prompt => true, :required => true,  :wrapper_class => 'radio-set'

