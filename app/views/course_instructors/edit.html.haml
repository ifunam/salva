%h3= t('.title')
= simple_form_for @course, :as => :course, :url => course_instructor_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), course_instructors_path
