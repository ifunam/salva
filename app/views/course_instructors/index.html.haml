.span-18.last
  %h3= t('.title')

#submit_buttons
  = link_to t(:new), new_course_instructor_path

#nav_links
  = link_to t('.my_courses'), course_instructors_path
  = link_to t('.not_my_courses'), not_my_course_instructors_path

= render :partial => 'search_form'

= render :partial => 'collection_header'

#collection_records
  = render :partial => 'record', :collection => @courses

= render :partial => 'collection_footer'

= paginate(@courses)

#submit_buttons
  = link_to t(:new), new_course_instructor_path
