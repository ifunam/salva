.section-with-border-and-bg
  .row
    = render :partial => "/shared/errors", :object => f.object.errors

  = render :partial => 'book_editions/form', :locals => { :f => f}
  .row
    = render :partial => 'bookedition_authors/form', :locals => {:book_author => @book_author, :f => f}
