%tbody{:id =>'collection'}
  = render :partial => 'record', :collection => @book_authors
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @book_authors, :id => 'ajaxed_paginator'