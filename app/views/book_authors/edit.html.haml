%h3= t('.title')
= simple_form_for @book_author, :as => :book_author, :url => book_author_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), book_author_path
