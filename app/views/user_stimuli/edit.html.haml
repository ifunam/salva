%h3= t('.title')
= simple_form_for @user_stimulus, :user_stimulus, :url => user_stimulus_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit t(:update)
    = link_to t(:cancel), user_stimuli_path
