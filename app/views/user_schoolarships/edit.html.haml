%h3= t('.title')
= simple_form_for @user_schoolarship, :name_key =>:user_schoolarship, :url => user_schoolarship_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), user_schoolarships_path