%h3= t('.title')
= simple_form_for @user_refereed_journal, :as => :user_refereed_journal, :url => user_refereed_journal_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), user_refereed_journals_path
