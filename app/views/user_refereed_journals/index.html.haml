.span-17.last
  %h3= t('.title')

#submit_buttons
  = link_to t(:new), new_user_refereed_journal_path

= render :partial => 'search_form'

#collection_header
  .span-1="&nbsp;".html_safe
  .span-11=t('.title')
  .action=t(:show)
  .action=t(:edit)
  .span1.last=t(:del)

#collection_records
  = render :partial => 'record', :collection => @user_refereed_journals

= render :partial => 'collection_footer'

= paginate(@user_refereed_journals)

#submit_buttons
  = link_to t(:new), new_user_refereed_journal_path
