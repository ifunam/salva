%div{ :class => 'span-7 section_left last' } 
  %div{:class => 'span-7 last'}
    = f.input :name, :as => :string, :html_input => { :size => '60' }
  %div{:class => 'span-7 last'}
    = f.input :url, :as => :string, :html_input => { :size => '60' }
  %div{:class => 'span-7 last'}
    = f.label :country, :required => true
    = f.collection_select :country_id, Country.all, :id, :name, {:prompt => true}
