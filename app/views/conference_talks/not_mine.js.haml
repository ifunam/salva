%tbody{:id =>'collection'}
  = render :partial => 'record', :collection => @conference_talks
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @conference_talks, :id => 'ajaxed_paginator'