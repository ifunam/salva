.span-18.last
  %h3= t('.title')

#submit_buttons
  = link_to t(:new), new_conference_talk_path

#nav_links
  = link_to t('.my_conference_talks'), conference_talks_path
  = link_to t('.not_my_conference_talks'), not_my_conference_talks_path

= render :partial => 'search_form'

= render :partial => 'collection_header'

#collection_records
  = render :partial => 'record', :collection => @conference_talks

= render :partial => 'collection_footer'

= paginate(@conference_talks)

#submit_buttons
  = link_to t(:new), new_conference_talk_path