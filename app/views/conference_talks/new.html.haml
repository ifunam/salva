= simple_form_for @conference_talk, :as => :conference_talk, :url => conference_talks_path, :html => { :multipart => true, :id => 'record'} do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:save)
    = link_to t(:cancel), conference_talks_path
