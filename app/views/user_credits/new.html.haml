%h3= t('.title')
= simple_form_for @user_credit, :url => user_credits_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:save)
    = link_to t(:cancel), user_credits_path
