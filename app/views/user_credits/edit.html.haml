%h3= t('.title')
= simple_form_for @user_credit, :user_credit, :url => user_credit_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), user_credits_path