.span-17.last
  %h3= t('.title')

= render :partial => 'search_form'

#collection_header
  .span-1="&nbsp;".html_safe
  .span-11=t('.credits')
  .action=t(:show)
  .action=t(:edit)
  .span1.last=t(:del)

#collection_records
  -#RMO add .order('year DESC, credittype_id ASC')
  = render :partial => 'record', :collection => @user_credits.order('year DESC, credittype_id ASC')

= render :partial => 'collection_footer'

= paginate( @user_credits)

#submit_buttons
  = link_to t(:new), new_user_credit_path
