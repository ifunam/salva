.section-with-border-and-bg
  .row
    = render :partial => "/shared/errors", :object => f.object.errors

  .row
    #language.span-8
      = f.association :language, :using => :name, :input_html  => {:class => 'chosen-select'}
      = link_to_new_record 'Agregar idioma', new_language_path

    #institution.span-10.last
      = f.association :institution, :using => :name , :input_html  => {:class => 'chosen-select'}
      = link_to_new_record 'Agregar institución', new_institution_path

  .row
    .span-8
      = f.association :spoken_languagelevel, :using => :name, :as => :radio, :wrapper_class  => 'radio_set'
    .span-10.last
      = f.association :written_languagelevel, :using => :name, :as => :radio, :wrapper_class  => 'radio_set'


