%h3= t('.title')
= simple_form_for @user_language, :user_language, :url => user_languages_path do |f|
  = render :partial => 'form', :locals => { :f => f }
  #submit_buttons
    = f.submit  t(:update)
    = link_to t(:cancel), user_languages_path