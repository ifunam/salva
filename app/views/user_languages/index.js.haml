%tbody{:id =>'collection'}
  = render :partial => 'record', :collection => @user_languages
  = render :partial => 'collection_footer'
%tfoot{:id => 'paginator'}
  %tr
    %th
      = will_paginate @user_languages, :id => 'ajaxed_paginator'
