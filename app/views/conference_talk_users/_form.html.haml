-  @conference_talk_users = user_role(conference_talk, :user_conferencetalks, current_user.id)
= f.simple_fields_for :user_conferencetalks, @conference_talk_users do |uf|
  - if !f.object.new_record?
    = uf.hidden_field :id
    = uf.hidden_field :conferencetalk_id, :value => conferencetalk.id
  = uf.hidden_field :user_id, :value => current_user.id
  = uf.input :roleinconferencetalk_id, :as => :radio, :collection => Roleinconferencetalk.all, :prompt => true, :required => true,  :wrapper_class => 'span-22 radio-set last'

