-  @user_projects = user_role(project, :user_projects, current_user.id)
= f.simple_fields_for :user_projects, @user_projects do |uf|
  - if !f.object.new_record?
    = uf.hidden_field :id
    = uf.hidden_field :project_id, :value => project.id
  = uf.association :roleinproject, :collection => Roleinproject.all, :as => :select, :input_html => { :class => 'chosen-select'}

