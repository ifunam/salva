.span-17.last
  %h3= "Informe anual de actividades - #{params[:year]}"

= render :partial => 'show_links'

= render :partial => 'profile', :object => @profile

= render :partial => 'section', :collection => @report_sections

= render :partial => 'show_links'
