<div class="required_legend"><%= get_label('required_legend') %>.</div>
<% form_tag({:action => 'update', :id => @edit.id}) do -%>
  <%= render :partial => 'form' %>
  <div id="operations">
  <%= submit_tag get_label('update') %> | <%= link_to get_label('back'), {:controller => 'navigator', :depth => 0} %>
  </div>
<% end -%>

